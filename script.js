const grid=document.getElementById("resourceGrid");const search=document.getElementById("searchInput");const count=document.getElementById("resultCount");const empty=document.getElementById("emptyState");let activeType="All",activeYear="All";function filtered(){const q=(search?.value||"").trim().toLowerCase();return RESOURCES.filter(r=>{const text=`${r.title} ${r.year} ${r.course} ${r.topic} ${r.type} ${r.description}`.toLowerCase();const course=document.body.dataset.course;return(!course||r.course===course)&&(!q||text.includes(q))&&(activeType==="All"||r.type===activeType)&&(activeYear==="All"||r.year===activeYear)})}function render(){if(!grid)return;const items=filtered();grid.innerHTML=items.map(r=>`<article class="resource-card"><div class="meta"><span class="tag">${r.year}</span><span class="tag">${r.topic}</span><span class="tag">${r.type}</span></div><h3>${r.title}</h3><p>${r.description}</p><a href="${r.file}" ${r.file==="#"?'onclick="return false;"':''}>${r.file==="#"?"Sample entry":"Open resource"} →</a></article>`).join("");if(count)count.textContent=`${items.length} resource${items.length===1?"":"s"} shown`;if(empty)empty.classList.toggle("hidden",items.length!==0)}search?.addEventListener("input",render);document.querySelectorAll("[data-filter]").forEach(b=>b.addEventListener("click",()=>{activeType=b.dataset.filter;document.querySelectorAll("[data-filter]").forEach(x=>x.classList.toggle("active",x===b));render()}));document.querySelectorAll("[data-year]").forEach(b=>b.addEventListener("click",()=>{activeYear=b.dataset.year;document.querySelectorAll("[data-year]").forEach(x=>x.classList.toggle("active",x===b));render()}));render();